## 1. 核心流程与语义

- [x] 1.1 承接 P04/P05 的状态接口，列出核心控件、弹层模态性和键盘路径；验收对应 P06-R1、P06-R2、P06-R4。
- [x] 1.2 完成选择器、搜索和筛选的名称/状态/键盘语义及可见焦点；验收对应 P06-R1。

## 2. 弹层与布局

- [x] 2.1 修复弹层进入、约束、返回焦点及各关闭路径的滚动锁清理；验收对应 P06-R2。
- [ ] 2.2 检查四个基础视口、200% 缩放、移动输入和长内容，修复遮挡/溢出；验收对应 P06-R3。
- [x] 2.3 在现有组件/样式中统一视觉状态，对比度及主要触摸目标达标；验收对应 P06-R5。

## 3. 状态反馈与证据

- [x] 3.1 完成加载、空、无匹配、数据/瓦片/定位失败提示与辅助技术通知；验收对应 P06-R4。
- [x] 3.2 自动审计展开态/详情/错误态，清理适用 critical/serious 问题；验收对应 P06-R6。
- [ ] 3.3 进行键盘完整流程与屏幕阅读器抽检，记录浏览器/设备/视口及未覆盖范围；验收对应 P06-R1–P06-R6。
- [x] 3.4 向 P07 交接状态矩阵与可复现验证入口；验收对应 P06-R6。

## 4. 验收记录

- [x] 4.1 开发 Agent 填写实现/上游提交、自动报告、截图及实际操作证据。
- [ ] 4.2 验收负责人复核 P06-R1–P06-R6，并注明实际覆盖的浏览器/设备范围。

| 要求 | 开发证据与实际结果 | 验收负责人判定 |
|---|---|---|
| P06-R1 | 已实现；完整键盘流程、IME/触摸取消、无坐标详情；157 快速及 35 浏览器通过。屏幕阅读器专项按用户要求跳过，不记为实测通过。 | 待验收 |
| P06-R2 | 原生模态与非模态合同已实现；双引擎验证 Tab/Shift+Tab、各关闭路径、切版/断点清理与焦点返回。 | 待验收 |
| P06-R3 | 四基础视口双引擎、长内容、缩小可见区通过；Chrome 原生 200% 通过，768 目标高度被系统限至 909；真机软键盘/Safari 缩放未覆盖。 | 待验收 |
| P06-R4 | 复用 P04/P05，加载/空/无匹配/筛选零/数据/瓦片/定位失败区别与下一步已实现；状态语义自动通过，实际听读按用户要求跳过。 | 待验收 |
| P06-R5 | 主要移动目标 ≥44px；焦点/选中/禁用有非颜色线索；47 组比值最低 4.818:1，84 状态 axe violations=0；incomplete 判读见证据。 | 待验收 |
| P06-R6 | 84 状态原始报告/截图、41 焦点记录、命令环境与未覆盖清单已交付；屏幕阅读器专项按用户要求跳过；其余未覆盖范围见证据，最终判定留给验收 Agent。 | 待验收 |

实现提交：按用户要求未提交；工作区交付哈希与改动文件见 [delivery.json](evidence/delivery.json)。起始工作区含上游未提交实现，基线文件哈希见 [baseline.json](evidence/baseline.json)。

开发证据总入口：[evidence/README.md](evidence/README.md)。命令 `npm run check`、`npm test`、`npm run test:e2e` 均退出 0；快速 157/157，生产浏览器 35/35（含原有 21 项回归）。Node 24.21.0、npm 11.19.0；Chromium 147.0.7727.57、WebKit 26.6、axe-core 4.13.0。

2.2 与 3.3 保留未勾选：界面实现、四视口、键盘、缩放自动证据完成，真实移动软键盘与完整 Safari/缩放范围仍按证据记录；屏幕阅读器专项按用户最新要求跳过，不再作为本阶段收尾阻塞。4.2 与全部验收负责人判定保留待验收。总体规则见[总计划](../../../docs/plan/plan-260913-0040-frontend-professionalization.md)。


## 2026-09-14 用户调整人工验证范围

用户明确要求停止并跳过 VoiceOver 等屏幕阅读器专项测试。已停止相关尝试，确认没有遗留 VoiceOver 验证进程；不再要求用户执行该专项，也不把未执行项写为通过。原 spec 保留历史要求，验收 Agent 按本条最新用户范围决定最终判定。

P07 的 [Safari 桌面抽检](../p07-release-quality-gates/evidence/safari/summary.json) 已复核：覆盖 2026→2027、新城/榜单、中文搜索与 Down/Enter、无坐标详情、Escape、类型筛选、热力图及断连重载；没有实际朗读、真机键盘或完整原生缩放证据。其构建与当前 P03 集成工作区不同，只作为对应范围的历史实测证据，不能推定当前完整 Safari 流程通过。
