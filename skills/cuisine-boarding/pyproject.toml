[project]
name = "cuisine-boarding"
version = "0.1.0"
description = "Data transformation pipeline for mapping raw cuisine labels to canonical taxonomy groups"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
